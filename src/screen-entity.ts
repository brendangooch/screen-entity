/**
 * a dynamic rectangle + collision detection + zIndex
 */

import { DynamicRectangle } from "@brendangooch/dynamic-objects";

export abstract class ScreenEntity extends DynamicRectangle {

    //

}